{
  "name": "nanopay-sdk",
  "version": "0.1.0.alpha",
  "main": "src/index.ts",
  "license": "Apache-2.0",
  "scripts": {
    "buildx": "rm -rf dist && tsc --project tsconfig.dist.json",
    "build": "rm -rf dist && node build.js",
    "docs": "typedoc src/index.ts"
  },
  "devDependencies": {
    "esbuild": "^0.13.12",
    "esbuild-plugin-globals": "^0.1.1",
    "typedoc": "^0.22.11",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "browser-or-node": "^2.0.0",
    "eventemitter3": "^4.0.7",
    "ky": "^0.28.7",
    "ky-universal": "^0.9.1"
  },
  "peerDependencies": {
    "bsv": "^2.0.10"
  }
}
